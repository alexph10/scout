# Copy to ~/.scout/.env to let `scout approve` star repos.
# Token scopes needed:
#   public_repo  - to star (scout approve)
#   no scopes    - search-only runs work fine

GITHUB_TOKEN=ghp_replace_me
